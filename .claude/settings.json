{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "if [ -f scripts/check-crate.sh ]; then scripts/check-crate.sh 2>&1 | tail -20; fi"
          }
        ]
      }
    ]
  }
}

# Argued surviving mutants.
#
# A surviving mutant means a test executed the line without constraining it —
# `testing.md` rule 15 calls that the characteristic failure of generated tests,
# and rule 17 says such a mutant is killed or argued.
#
# ⚠️ **Killing is the default.** An entry here is a claim that the mutation
# produces behaviour genuinely indistinguishable from the original, not that
# writing the test was inconvenient.
#
# ⚠️ **This is a list nobody grows quietly.** It is reviewed at every milestone
# boundary, the same discipline `baselines/review.txt` and `baselines/unsafe.txt`
# carry, and a baseline that only grows means the tests are losing.
#
# Format: <file>:<line>:<col>: <mutation>  <reason>
#
# ⚠️ The reason is required. A location on its own suppresses a survivor while
# recording nothing about why.
#
# Empty as of `M0.17`: the first full run over `oqueue-core` found 11 survivors
# and all 11 were killed by writing tests rather than argued here. That is the
# ratio to keep.
crates/oqueue-core/src/bundle.rs:50:9: replace RegionAlg::code -> u8 with 0  RegionAlg has one variant, None, whose code *is* 0 -- so returning 0 unconditionally and returning `self as u8` compute the same byte, and no test can separate them. `M8` adds the second variant (doc 10 #40 puts the field in the footer from M3.13 precisely so it can), and this entry comes out then rather than being re-argued: the mutant becomes killable the moment a second code exists, and `from_code` already refuses every value this build does not know.
bin/oqueue/src/serve.rs:38:5: replace serve with ()  `serve` binds a listener and `accept_loop` races it against the coordinator's task; both run until the process is killed, so no in-process test can call either and return. ⚠️ **These mutants are killed, by a gate `cargo mutants` cannot run**: `scripts/kafka-client-harness.sh` starts the real binary, fails within ten seconds if it never prints `listening on`, and then round-trips librdkafka and the Java client through it -- so a `serve` that did nothing, or an `accept_loop` that returned `Ok(())` at once, fails there. These entries record that the coverage exists elsewhere, not that the lines are unconstrained. Every decision either function makes that *can* be tested has been lifted out beside it -- `store_for`, `durability_warning`, `build_cluster`'s composition, `advertised_identity`, `SERVE_LIMITS` -- and each has a test; what is left is the loop.
bin/oqueue/src/serve.rs:128:5: replace spawn_connection with ()  `M4.18` split the per-connection spawn out of `accept_loop` so the TLS handshake happens on the connection's own task rather than in the accept loop. It has the same shape as `serve` above and the same coverage: it spawns and returns, so no in-process test can observe it, and `scripts/kafka-client-harness.sh` is what kills it — six legs round-trip real librdkafka and Java clients through this function on every run, one of them over TLS, so a `spawn_connection` that did nothing fails all of them. ⚠️ `accept_loop`'s own entry was dropped here rather than renumbered: it is killed now by `the_accept_loop_notices_a_dead_coordinator_rather_than_serving_past_it`, and a baseline line for a mutant that no longer survives is a suppression nobody needs.
crates/oqueue-core/src/group_state.rs:132:9: replace MemberEpoch::get -> i32 with 0  MemberEpoch has no constructor beyond INITIAL (0), so every value this build can construct is 0 and returning 0 unconditionally computes the same answer as returning `self.0`; no test can separate them. ⚠️ **This entry is why `M4.32` exists, and it got the re-key wrong twice.** It was keyed `113:9` for the whole of M4 while the mutant emitted elsewhere — invisible, because `check-mutants.sh` only ever walked survivors asking whether each was argued. `M4.32` added the converse loop and then re-keyed this entry to `124:9` **from the stale prose in its own backlog row rather than from the 6-hour run it had just produced**, and marked it `unviable:` on the strength of a measurement taken before `M4.34` and `M4.39` moved the file. The full run says `132:9`, and says MISSED. Review of that commit caught it; the gate now also refuses an `unviable:` claim whose location matches a survivor, which would have caught it too. ⚠️ **Its discharge condition is a decision, not a task**: the entry says it comes out when a second constructor exists, `M4.7`/`M4.9` both shipped without one, and whether to build the member epoch or amend `ADR-0033` is the argued finding `b0b5af3ced25`.
crates/oqueue-broker/src/sync_group/barrier.rs:258:26: replace > with >= in SyncGroups::refuse  The two differ only when the cell already holds a refusal for *this* generation: `>` rewrites it with an identical `Assignment { generation, map: None }` and notifies again, `>=` returns early. No observer can tell. A follower arriving after the first refusal reads `Known::Refused` without parking, so it needs no wake; one parked before it was already woken by the first refusal, and `Notify::notify_waiters` wakes only waiters registered at the time — a second call with none registered is a no-op. Its neighbours are all killed: `==` and `<` on the same comparison, and `||` to `&&` between the two disjuncts. ⚠️ **Not respectively** — an earlier version of this entry mapped each to one named test, and review measured that `||` to `&&` reddens `a_late_refusal_does_not_reopen_a_later_generations_refusal`, `a_refusal_does_not_overwrite_a_later_generations_assignment` and `a_refusal_does_not_unpublish_this_generations_own_assignment` together rather than one of them; that enumeration also still described the pre-fix guard, which had a bare `map.is_some()` disjunct this one does not. What survives is the one comparison with no behavioural difference, not an unguarded branch. ⚠️ `check-mutants.sh` matches by location prefix only, so the prose half of an entry is checked by nothing — the same undetectable staleness the re-keying note below warns about, one field over. ⚠️ This entry comes out if `refuse` ever carries a payload that differs between calls — a reason string, a timestamp — because then rewriting and not rewriting stop being the same write. ⚠️ **This entry has been re-keyed five times** — `sync_group.rs:258:43`, then `:264:26` when the guard gained a conjunct, then `barrier.rs:224:26` when the file split at `code-structure.md`'s 500-line limit, then `:233:26` when a review round asked for a doc comment above it, and now `:258:26` — `M4.57` gave `Assignments` a newtype and an accessor seventeen lines further up, and `M4.55`'s own doc-comment edits in this file moved it eight more. ⚠️ **The fourth and fifth are the clearest: neither touched this code at all.** A comment, and then a type one level out, moved a suppression's key. ⚠️ **And the fifth was caught by `cargo mutants --list`, not by the mutant surviving** — `--list` enumerates every mutant's location without running a single test, in seconds rather than the six hours `--full` costs, so a key can be re-derived whenever the file moves instead of waiting for the next full pass to report it as an unargued survivor. `M4.55`; the four before it were each found the slow way. ⚠️ **Run `--list` *after* the edit, not before.** `M4.55` re-keyed this entry to `:250:26` from a listing taken before its own doc-comment edits to the same file, and review caught the result stale on arrival. It *would have been* the sixth instance of what this entry narrates, in the commit correcting it — nothing was committed, and an earlier wording here said otherwise while `check-mutants.sh` said this, so the two copies disagreed in the one entry whose subject is a claim no gate can check (`M4.62`). The key is the last thing to settle in any change that touches `barrier.rs`. That is `M4.32`'s own subject made concrete: `check-mutants.sh` matches survivors against this list by location prefix and never the converse, so a stale key suppresses nothing and no gate can see that it has gone dead — the only reason all three were caught is that the mutant kept *surviving*, which made the mismatch visible as a failure. An entry whose mutant became killable would have gone quiet instead. `M4.43`.
crates/oqueue-compact/src/merge/outcome.rs:28:9: replace MergeOutcome::puts -> usize with 1  `merge` writes exactly one output object, so the field it reports is constructed as `1` at its single call site and returning the literal computes the same answer as returning `self.puts`; no test can separate them. ⚠️ **`M5.6` was named as this entry's discharge and did not discharge it**, which is recorded here rather than left for the next full run to rediscover: the multipart writer makes a merge's *request* count a function of the part size, and this field counts **objects** — one merge, one round, one object, however many parts carry it. The entry's real discharge is a caller that writes more than one object in a run, which is `M5.13`'s commit deciding an output's identity or a later round-splitting change; until one exists the literal and the field compute the same number. ⚠️ An entry whose stated discharge passes without discharging it is the failure mode the `barrier.rs` entry below narrates for *keys*, one field over: `check-mutants.sh` matches by location and reads no prose, so nothing would have said this. It is kept as a field rather than dropped because `M5.4`'s own acceptance asks that `CostEstimate::of` predict the run's GET *and* PUT counts, and a field that exists only in the estimate cannot be compared against what the run did. ⚠️ **Re-keyed once already, before it was ever committed**: the type moved to `merge/outcome.rs` when `merge.rs` hit the 500-line limit in the same task, which is the fifth instance of what the `barrier.rs` entry above narrates — a key that moves because a file split, not because the code changed.
crates/oqueue-store/src/s3_stream.rs:61:9: replace <impl core::fmt::Debug for StreamingWriter<'_>>::fmt -> core::fmt::Result with Ok(Default::default())  A `Debug` impl that prints nothing the workspace asserts, on a type only a live backend can construct: `StreamingWriter` is built inside `S3Store::open_multipart`, whose body needs a real endpoint (`check-coverage.sh`'s `ALWAYS` entry for this crate records the same fact for the same reason). ⚠️ **It deliberately renders almost nothing** -- `finish_non_exhaustive` over one counter -- because `S3Store`'s own `Debug` says why: a derive on the vendor client risks dumping request-signing state. A test asserting its exact text would be asserting that decision's formatting, not its content. `M5.45` runs these writers against MinIO; this entry comes out if that run gives the type a behavioural assertion.
crates/oqueue-store/src/s3_stream.rs:97:27: replace == with != in <impl oqueue_core::MultipartWriter<'store> for StreamingWriter<'store>>::finish  The empty-stream guard (`self.bytes == 0`) on a code path no in-process test reaches: `finish` is only callable on a writer `S3Store::open_multipart` returned, which needs a live endpoint. ⚠️ **The same guard is tested** -- against `FakeObjectStore`, by `a_streamed_object_with_no_parts_is_refused` in `oqueue-core`'s `store_streaming.rs`, which is the conformance-level statement of the contract both implementations are held to. What is unreachable here is this crate's copy of it. `M5.45` is the row that exercises it against MinIO.
crates/oqueue-store/src/gcs.rs:233:9: replace <impl core::fmt::Debug for StreamingWriter<'_>>::fmt -> core::fmt::Result with Ok(Default::default())  `s3.rs:264:9`'s entry, for the same type in the GCS backend and for the same reason.
crates/oqueue-store/src/gcs.rs:269:27: replace == with != in <impl oqueue_core::MultipartWriter<'store> for StreamingWriter<'store>>::finish  `s3.rs:292:27`'s entry, for the same guard in the GCS backend and for the same reason. ⚠️ **Re-keyed once before landing**, when `s3.rs` hit the 500-line limit and the writer moved to `s3_stream.rs` in the same task — the `barrier.rs` entry's own subject, for the sixth recorded time.
crates/oqueue-compact/src/read_amp/survey.rs:104:51: replace < with <= in ReadAmp::before_tail  The filter answers "is there a history portion at all" before reporting the trimmed range, and `lo == boundary` is the case where the range's first whole object is already a tail object. With `<=` the trimmed range is reported as `Some((lo, lo))` instead of `None` — and both produce the same plan, because an empty range holds no records, `objects_needed` is 0 and `ratio` is `0.0` by its own documented convention, which is at or below any threshold. So the two spellings differ only in a value no caller can act on differently. ⚠️ **Kept as a branch rather than deleted** — the repository's usual answer to an indistinguishable branch — because what it states is the type's contract: `covered` is `None` when no object lies wholly inside, and a `Some` naming an empty range would make every later reader of `covered` prove that emptiness again. `M5.16`, which gives the guard timestamps instead of the tier boundary, is where this arithmetic is rewritten and the entry expires. ⚠️ **Re-keyed once before it was ever committed**, from `98:51`, when the accessor above it gained the doc `M5.46`'s own review asked for — a key that moves because the file moved, not because the code changed, which is what the `barrier.rs` entry above narrates.
crates/oqueue-compact/src/read_amp/walk.rs:97:25: replace |= with &= in Walk::visit  Whether an object is in the tail tier is folded across its spans with `|=`, and no index this workspace builds can tell that from `&=`: an object's spans for one partition arrive in **one** `BatchCommitted` entry, so `IndexState`'s two-tier fold puts all of them in the same tier and `batch.bytes().is_some()` is the same answer for every span of an object. The two spellings differ only for an index that split one object's spans across entries in different tiers, which the fold cannot produce. ⚠️ **Kept as `|=` rather than simplified to the first span's answer**, because the direction matters if that ever changes: one tail span makes the object tail data, and the age guard exists to refuse rewriting tail data — `&=` would compact it. `M5.16`, which replaces the tier test with timestamps, is where this disappears.
crates/oqueue-broker/src/read/manifest.rs:121:44: replace < with == in Cluster::manifest_batches  The branch decides whether a search miss means "older than this link" and so worth a chain hop. `start == first.base_offset()` cannot reach it: `position` would have found that offset and returned before the check. The only path that reaches it with equality is an entry whose own `end_offset` overflows -- a manifest the writer produced wrongly -- and there both answers are a miss, one after a wasted GET. A test discriminating them would have to write a corrupt manifest to assert which flavour of "not found" it gets.
crates/oqueue-broker/src/read/manifest.rs:121:44: replace < with <= in Cluster::manifest_batches  `crates/oqueue-broker/src/read/manifest.rs:87:44`'s entry, re-keyed after `M5.63`'s first round moved the line, and for the same reason: the two spellings differ only when `start` equals the manifest's first base offset, which `position` would already have found and returned on.
crates/oqueue-compact/src/layout.rs:123:64: replace match guard log_start > cursor.get() with true in PlannedInputs::derive  `IndexState::find_batches` returns `BelowLogStart` only when `start < log_start` -- that comparison is the refusal's own condition -- so in every arm this guard can see, `log_start > cursor` is already true and replacing it with `true` computes the same branch. The guard exists for the index that is *not* this one: a `MaterializedIndex` whose refusal carried a start at or below the cursor would send the loop back to the same cursor forever, and a loop whose only bound is a comparison is the one `PartitionIndex::push`'s doc names as the worse way to find out. The `false` mutant is killed by `a_trim_between_plan_and_derivation_resumes_at_the_log_start`. `M5.19`.
crates/oqueue-compact/src/read_amp.rs:105:56: replace match guard log_start > start.get() with true in read_amp  The same equivalence as the `layout.rs` entry above, for the same reason: the refusal is produced only when the requested offset is below the log start, so the guard is always true where it is evaluated. It is kept so that a refusal from some other index carrying a start at or below the request cannot be retried into the same refusal; here that would be one wasted walk rather than a livelock, since the retry is not a loop. The `false` mutant is killed by `a_sweep_over_a_trimmed_partition_plans_from_the_log_start`. `M5.19`.
crates/oqueue-compact/src/layout.rs:123:74: replace > with >= in PlannedInputs::derive  Equivalent for the reason the `true` entry for this guard gives: the refusal is produced only when the cursor is strictly below the log start, so `log_start >= cursor` and `log_start > cursor` never differ where the guard is evaluated. `M5.19`.
crates/oqueue-compact/src/read_amp.rs:105:66: replace > with >= in read_amp  Equivalent for the same reason as the `layout.rs` entry above: the refusal carries a log start strictly above the requested offset by construction. `M5.19`.

# Argued review findings.
#
# A blocking review finding is resolved in exactly one of two ways (doc 21 §6):
#
#   1. Fixed   — the diff changes, the hash changes, the review re-runs
#   2. Argued  — an entry here naming the finding and why it is not a defect
#
# ⚠️ When the verdict is `changes-requested`, every **major** finding needs the
# same treatment. Otherwise a reviewer asks for changes, the commit lands, and
# the findings survive only in a gitignored directory.
#
# ⚠️ The entry must be **staged**. An unstaged line is ignored: it would suppress
# a finding while leaving nothing in the commit to show for it.
#
# Format: <id>  <reason>
#
# ⚠️ The reason is required. An id on its own is refused, because it suppresses
# a blocking finding while recording nothing about why — which is exactly the
# quiet growth this file exists to prevent.
#
# ⚠️ **This file serves two reviews, and their ids are derived differently.**
# Nothing in a line says which, so a reader re-deriving an id needs to know:
#
#   per-commit    `scripts/review.sh record` prints it; `check-reviewed.sh`
#                 prints it again when one is unresolved.
#                 id = sha256(file + "\0" + summary), truncated to 12 hex.
#   milestone     `scripts/milestone-review.sh record` prints it;
#                 `check-milestone-review.sh` prints it again when unresolved.
#                 id = sha256(kind + "\0" + summary), truncated to 12 hex.
#                 ⚠️ A cross-cutting finding has no single file, which is why
#                 its id cannot use one — that is the whole point of the outer
#                 loop. Both gates match this file's lines undifferentiated.
#
# Either way the id is stable across unrelated edits, so arguing a finding does
# not have to be repeated every time the diff moves for some other reason. ⚠️ A
# milestone finding is argued when its right outcome is **not** a backlog row —
# typically `kind: spec-wrong`, where the outcome is a decision. Say which
# review a line came from in its reason; nothing enforces that.
#
# ⚠️ This is a file nobody may grow quietly. A growing argued-list is itself a
# finding: it means a reviewer is being systematically overruled, and one of the
# two sides is systematically wrong. Read it at every milestone boundary.
#
# Empty is the correct state.

# ── M0 checkpoint review, 2026-08-15 ────────────────────────────────────────
b501a554054b  M0 sat at sdd.md's 20-task cap, so the review had no legal output. Resolved by amending the cap rather than by splitting M0 or ignoring the finding: the cap's own rationale is "two milestones wearing one name", and M0 is named "Workspace, contracts, and quality gates" — its gate tail is the third thing it is named for. sdd.md now says the number is a heuristic and that exceeding it costs a recorded argument; this is that argument. M0.20 and M0.21 are the rows it permits.
f44f87664480  security.md's fuzz.sh (rule 5) and check-secrets.sh (rules 6-7) are named by a standard and scheduled nowhere. Not a backlog row, because neither belongs to M0: a decoder fuzz target needs a decoder (testing.md rule 24 puts it with M2) and a secrets gate needs secrets (M8). Recorded instead in roadmap.md's "Deferred into a later milestone" table, which exists for exactly this and which the finding correctly notes was the thing nobody updated.

# ── M0's boundary review ────────────────────────────────────────────────────
#
# ⚠️ Five majors argued rather than made M0 rows, because each belongs to a
# milestone that has not started and M0 is already over `sdd.md`'s task cap
# with a recorded argument. ⚠️ **An argument that only lives here is the
# failure `M-1.47` names**, so every line below names where the obligation
# actually lives — four in `M1.md`, and the fifth in `roadmap.md`'s deferral
# table, which is the mechanism this project already uses for an obligation
# with no owning milestone yet. This file records that the review saw it land
# there; it is not itself the record.
8a292bf49bcd  NFR-56's exemption becomes unfailable as compile cost grows, and M1 is the milestone that adds the compile cost — M1.md's "Inherited from M0's boundary review" carries it
59eed6d3205d  security.md's applies_to routes it to none of the code holding secrets; M1 writes the first code carrying credentials, and M1.md's "Inherited" section carries it
a4d96f6ba946  whose enum holds a seam's errors is an ADR, not a task — M1.md's "Decisions required first" now lists it and blocks task 3 on it
bcf5d6f697f2  spec-wrong, and per the milestone-review skill that is a decision rather than a task: review.md rule 15 records the tension with rule 16 openly and roadmap.md's deferral table carries the decision into M2, since the fix is a procedure whose owner is not this review
791bd61fbe20  three copies of one TOML parser, reachable by the first [[bench]] and by a [target.*.dependencies] table; M1 adds the first real dependency tree and M1.md's "Inherited" section carries it
f6d93488a523  M2.28's backlog row read `todo` although its own commit (cc5e298) was on main and every task built on it (M2.29-M2.34) already read `done` -- fixed directly in the same commit that recorded this review by flipping the row to `done`, so no task remains for anything to act on
8d5795399752  spec-wrong, and per the milestone-review skill that makes it a decision rather than a task -- discharged as a correction in the same commit that recorded this review (M3.19) rather than scheduled: FR-10 says "durable in object storage" and says nothing about the metadata log, so what overclaimed was backlog.md's own M3 section header (the binding artifact), M3.md's Goal and ADR-0020's prose, all three now corrected. Pulling doc 10 #12's engine into M3 was the alternative and is declined for the reason ADR-0020 point 5 already gives -- doc 13 6 asks for a benchmark that has not been run. The residue is a durable metadata log, recorded in roadmap.md's deferral table and received by M6.md task 7a, so nothing is left for a row to act on in M3
0503718c1131  spec-wrong, and per the milestone-review skill a decision rather than a task -- discharged as a correction in the same commit that recorded this review (M3.25) rather than scheduled. sdd.md's freeze rule and M3.19's binding-artifact rule were both right and could not both hold, so the freeze gains exactly one exception: an acceptance criterion the code does not meet is corrected in place, in the commit that corrects it, which names itself in the cell. Everything else about a done row stays frozen. The alternative -- leave the criterion false and record the correction only in a commit message -- was declined for the reason M3.19 already gave: next-task, the completion gate and every later task read the backlog, not git log, so a correction that lands only in history leaves the claim standing where it is actually read. An acceptance criterion is a claim about the code rather than a record of what someone intended, which is what makes this case different from every other edit the freeze forbids. M3.38 is the first row to use it, on M3.22's cell

# ── M11.4 per-commit review ─────────────────────────────────────────────────
a19c2a2289e1  crates/oqueue-broker/src/init_producer_id.rs: mint_producer_id/fold guarantees ProducerId uniqueness only within one process. Two different broker processes minting their first identity (MINTED at 0 in both) in the same nanosecond can fold to the same value -- real, but not exploitable in today's deployment shape for the same reason cluster.rs's own node_id gap needs no fleet answer yet ("A single-node broker has a single honest identity; the fleet answer belongs to the milestone that has a fleet", never given a deferral row of its own, M2.22's row: "Broker identity (node ID, hostname) chosen deliberately, not by convenience"). The one real fix -- mixing a per-broker identity into the fold -- is not available: Cluster::node_id is hardcoded 0 for every process today, so threading it in now would mix in a constant and change nothing, which would be a fix in appearance only. mint_producer_id's own doc now states the single-process-only guarantee plainly (it previously implied more than it delivered) rather than papering over the gap; it closes the moment a real per-broker id exists, the same milestone that answers node_id's own open question. No existing backlog task_id fits (no milestone currently plans a broker fleet), so this is argued rather than scheduled -- the same treatment the identical class of gap already received for node_id.

# ── M4 closing milestone review ─────────────────────────────────────────────
b0b5af3ced25  spec-wrong, and per the milestone-review skill that makes it a decision rather than a task. `MemberEpoch` is dead code at the end of M4 and `ADR-0033` decision 2 asked for the three-epoch model "from the first commit" so a KIP-848 pickup would be a coexistence addition rather than a rewrite. Two answers exist and neither is a task the backlog can carry: build a real per-member epoch into `GroupRecord` now, for a protocol `ADR-0033` itself deferred post-v1 and which nothing in v1 reads -- speculative structure the decision's own "not a rewrite" argument does not buy at this cost; or amend `ADR-0033` to say the classic protocol needs two epochs and the third arrives with the protocol that needs it. Recorded here rather than scheduled because choosing is `adr` work, and the choice changes what M4's 32 commits were for. ⚠️ **Receiver: `roadmap.md`'s "Deferred, with nothing scheduled" paragraph**, which already carries FR-22 for the same reason — the obligation belongs to whoever picks up KIP-848, and `sdd.md` refuses a deferral naming no receiver, so it goes to the paragraph that exists for deferrals with no receiving milestone rather than to a milestone chosen to have one. ⚠️ The stale doc at `group_state.rs:28-33` (it says `M4.2`'s `GroupCoordinator` holds one per member and `M4.9`'s `Heartbeat` advances it; neither does) is not part of the decision and is `M4.39`'s.
e983d58d0dbd  spec-wrong, and a decision rather than a task. M4's plan serves FR-22, which `ADR-0033` deferred in M4.0 -- the milestone's own first commit -- and eleven frozen rows cite FR-22 for work that is FR-20's wording. ⚠️ **No part of this is scheduled, including the part that looks editable.** `roadmap.md:180`'s M4 cell keeps FR-22 deliberately, with `M4.22`'s annotation saying why the citation is not delivery, and warns in the same cell that `check-requirements-trace.sh` compares its id set against `M4.md:12` — so striking it from one file fails that gate and striking it from both reverses `M4.22` and `M4.24`. The eleven frozen rows are the part with no legal move at all, because `sdd.md`'s freeze has exactly two exceptions (a criterion the code does not meet, a rendering repair) and a wrong requirement citation is neither. The choice is between leaving eleven rows permanently wrong and widening `sdd.md`'s exception set, and the second is a standards change with its own argument -- the docs lens proposed replacing the growing bullet list with one test ("a frozen row may change only where the change makes the claim already in it true, or visible"), which is `sdd.md` work and not M4's. ⚠️ **Receiver: `roadmap.md`'s "Deferred, with nothing scheduled" paragraph**, beside the FR-22 deferral this finding is about.
f354215b9db6  ⚠️ **WITHDRAWN — this finding is wrong, and M4's addendum review disproved it.** It claimed NFR-56 has no reachable failure path because `check-budget.sh`'s compiling exemption is the only branch that runs. `check-budget.sh:260` is `compiling_n > 0 && warm_ms <= BUDGET_MS` — **two** conjuncts — and falls through to `fail` at `:327` when the warm remainder alone exceeds the budget, which `tests/gates/negative.sh` pins with `setup_budget_over` and `setup_budget_eroded_behind_a_build`; the script's own cost table lists three erosion rows reaching `-> fail`. `ADR-0029:109` records exactly this as the decision ("the next dependency, gate, or host that pushes the sum itself past 10000 ms is a genuine NFR-56 violation under this reading"), so the reading the finding called a loophole is the gate working. The frequency of the exemption is real and is what the gate already warns about on every run; it is not the same claim. ⚠️ **And the exits were never unavailable**: `check-budget.sh:78` names them — make the suite faster, or move work to CI and say so — and calls both tasks, which M4 filed twice as `M4.19` and `M4.20`. Recorded as withdrawn rather than deleted, because a line that vanishes leaves no trace that the argument was made and beaten.
35f264857aa6  spec-wrong, and a decision rather than a task. `ADR-0016` rule 2 asks for terse frozen rows and records ~69,000 words of `backlog.md` as the problem it was written to fix; the file is 128,479 words at `fced761`, larger than the research corpus, and M4's median Notes cell is 442 words against M2's 32. The ADR's own alternatives say "generate the backlog from commit history -- deferred, not rejected. Worth revisiting if rule 1 fails to hold in practice", and rule 1 has not held for four consecutive milestones (M1 265, M9 306, M11 281, M4 442). That trigger names a successor decision, not a sweep row: rewriting 44 M4 rows would itself be forbidden by the freeze, and a rule nobody has followed since M3 is not repaired by one more milestone trying harder. ⚠️ **Receiver: `roadmap.md`'s "Deferred, with nothing scheduled" paragraph**, as a successor to `ADR-0016` that the ADR's own alternatives already name. ⚠️ M4's section notes now carry the `sdd.md` 20-task-cap argument the standard asks for, which is the half of this finding that did have a legal move.

# ── M4 addendum milestone review ────────────────────────────────────────────
0230305d9925  fixed directly in the commit that recorded this review (`M4.40`) rather than scheduled, the same discharge `f6d93488a523` above uses -- so no task remains for anything to act on. M4's `sdd.md` cap argument called `M4.3` and `M4.4` dissolved when the table has both `done`, omitted the one genuinely dissolved sibling (`M4.15b`), and left `M4.18` in no bucket, so its five buckets covered 43 ids while stating 44. All three are corrected in place and the buckets now partition all 45 ids, `M4.40`'s own row included -- which review of that commit had to point out in turn, `M0.32`'s "the cap argument covers **through this row**" being the rule a repair that adds a row keeps breaking.
e4f7daa6e50b  fixed directly in the commit that recorded this review (`M4.40`). ⚠️ **The finding is right and this file's own header is where it is written**: "an argument that only lives here is the failure `M-1.47` names, so every line below names where the obligation actually lives". M4's four argued entries named none. Three now carry a `Receiver:` clause pointing at `roadmap.md`'s "Deferred, with nothing scheduled" paragraph, and that paragraph now records all three -- `ADR-0033`'s third epoch, FR-22's citation on eleven frozen rows, and a successor to `ADR-0016`. The fourth is `f354215b9db6`, which is withdrawn rather than received, for the reason the next line gives.
c4ad486b3110  fixed directly in the commit that recorded this review (`M4.40`), and the fix is a **withdrawal**: the finding it names is correct and the argued entry it examined (`f354215b9db6`, above) was wrong. NFR-56 does have a reachable failure path -- `check-budget.sh:260` is a two-conjunct condition falling through to `fail` at `:327`, pinned by `setup_budget_over` and `setup_budget_eroded_behind_a_build` in `tests/gates/negative.sh`, and `ADR-0029:109` records that reading as the decision rather than as a loophole. ⚠️ **Recorded as withdrawn in place rather than deleted**: a line that vanishes leaves no trace that the argument was made and beaten, and this project has now made the "NFR-56 is a preference" claim three times across M0, M10 and M4.

# ── M4 third boundary review ─────────────────────────────────────────────────
0391bb840fa7  fixed directly in the commit that recorded this review (`M4.64`) rather than scheduled, `0230305d9925`'s own discharge above. ⚠️ **It could not have been scheduled**: `M0.32`'s rule makes the cap re-argument owed by any commit that adds a row, this commit adds eight, and `M4.64` -- the row for it -- is `done`, which `record` refuses as a `task_id`. The argument now partitions all 76 ids, and the bucket that grew is the gates one, from three to nine.
161d39ad936f  fixed directly in the commit that recorded this review (`M4.64`), and the fix is a **deletion**: the "deliberately terse" sentence was a self-description nothing checks, which is what `M4.63` established goes rather than gets another edition. The measurement it was hiding (median 406 across `M4.31`-`M4.63`, the nine rows before `M4.64` averaging 908) is in its place, and the rule itself is already routed to a successor ADR by `35f264857aa6` above -- so scheduling a row here would file a second task against a decision someone else owns.
f70cd81b6d4e  spec-wrong, and a decision rather than a task -- `35f264857aa6` above routes `ADR-0016` rule 2 the same way, and this is its sibling. `review.md` rule 15a caps a change at two review rounds and M4 exceeded it in eight of seventeen commits, every one on prose that rule 9a itself makes `minor`; the finding's own measurement is that M4 reproduced `ADR-0016`'s argument for the cap exactly, with the two protocol defects a real consumer hits found by this boundary round rather than by any of those rounds. Either the cap is wrong for repair-and-sweep commits or M4 broke it eight times, and choosing is `adr` work beside the rule-2 successor, not a repair a review may make. ⚠️ Nothing can gate it either way: `check-reviewed.sh` binds a hash, not a round count.

# ── M4 fourth boundary review ────────────────────────────────────────────────
3ab2f215707b  spec-wrong, and a decision rather than a task, which `milestone-review/SKILL.md`'s own "Then re-plan" reserves for exactly this: the rule found wrong is the disposition rule `M4.73` wrote three commits ago, and re-specifying it here — inside the round whose answers it would change — is the quiet re-specification that section forbids. ⚠️ **The finding is right.** The reopen test admits a client-visible change or a gate that cannot fail, and "evidence that does not constrain what it is cited for" is neither, yet it is the class this milestone hit most: `M4.52` (two `check-mutants` cases green on `profile 'mutants' is not defined` from `M4.19` to `M4.32`), `M4.32`, `M4.60`, `M4.69`, and `M4.78` in this very round — which the gates lens could only disposition by reasoning around the wording. It is non-negotiable 3's property one layer out: a milestone closing while citing evidence nobody has watched fail. The candidate amendment is one clause, *or the milestone's own evidence does not constrain what it is cited for*, and it belongs beside `ADR-0016`'s successor in `roadmap.md`'s "Deferred, with nothing scheduled" with the other two rules this milestone measured as wrong. ⚠️ Acting conservatively in the meantime rather than waiting for the decision: `M4.78` is taken as a reopen although the rule as written hands it on.

# ── M8 closing milestone review ──────────────────────────────────────────────
6b4b1a6ed687  spec-wrong, and a decision rather than a task. M8's acceptance explicitly requires customer-key produce to fail closed when the shipped composition has no configured sealer: `M8.8` says it "refuses produce rather than writing plaintext", and ADR-0056 records `RejectingRegionSealer` as the safe default until a deployment supplies a real sealer. Wiring cloud credentials into `bin/oqueue` is outside M8's simulated-provider scope and would change the fail-closed default the milestone is required to preserve. The roadmap now states this boundary directly; future deployment wiring, if product scope requires it, needs its own decision and receiving task rather than being smuggled into M8's closure.

# ── M6 closing review ────────────────────────────────────────────────────────
80a0ec2c79ca  fixed, not a task: `M6.17` (060d6cd) makes a coordinator replay from a freshly opened view under each lease term before its first write, and `a_successor_that_booted_early_loses_nothing` reproduces the loss this finding described (offset 10 where 20 was due) and passes. The finding reopened M6 and was worked in it, which is why it names no open row.
d2732bffb5a8  fixed in part, and the rest is `M6.19`: M6's second closing round found this scripted handover still has no load and no pause, so it cannot catch an append held past the lease — that case is `M6.19`'s, which the round recorded against it. What `M6.17` added is `a_successor_that_booted_early_loses_nothing` — a leader, a node booted during its term, a prune, a lease handover, and every acknowledgement checked — added to `m6-complete.sh` by `M6.17` (060d6cd).

[package]
name = "oqueue-crypto"
description = "AEAD, envelope encryption, the DEK cache, and nonce construction."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

# ⚠️ `oqueue-core` and nothing else in this workspace — `architecture.md`'s star
# topology, enforced by `check-layering.sh`. A dependency on a sibling crate is a layering violation, not a shortcut.
[dependencies]
oqueue-core = { path = "../oqueue-core" }

# ⚠️ Empty on purpose, and worth a word because it is the one section
# `check-layering.sh` never reads: nothing here may take an async runtime.
# `M0.16` measures NFR-56's floor on a workspace that compiles none, and the
# seam's futures are driven by `Waker::noop()` and a busy poll instead.
[dev-dependencies]

[lints]
workspace = true

---
title: "M-1 — AI development system"
description: >
  Read when working on M-1. The system that builds everything else: standards, gates, skills, and the two loops.
tags: [product, milestones, planning, m-1]
---

# M-1 — AI development system

**Kind:** AI-native development support ·
**State:** in progress ·
**Serves:** NFR-50, NFR-54

## Goal

Every later milestone is executed by an agent against a set of rules, and **a
rule that arrives after the code it governs has already been violated**. So the
rules, the scripts that enforce them, and the loops that invoke those scripts
come first. Nothing in this milestone is broker code.

After it, an agent can be handed a milestone and drive it to completion
unattended: each task becomes one reviewed commit that leaves the tree green,
each commit is read by an agent that did not write it, and the milestone as a
whole is read once more at its boundary. Nothing in that chain is satisfiable
by claiming it happened.

## Depends on

Nothing. It is the first milestone by construction.

## Decisions required first

None outstanding. Doc [21](../../../researches/21-ai-development-loop.md) is
already a position rather than research and carries this milestone's decisions:
the context-isolated reviewer (§4), the artifact-keyed gate (§5), the allocation
rule that anything a script can decide is never an agent's job (§3), and the
promotion of mutation testing to the primary anti-slop gate (§7).

## Tasks

⚠️ **This milestone is decomposed, not planned** — it is the current one, so
[`backlog.md`](../backlog.md) is authoritative and this file does not restate
its 44 tasks. What follows is the shape.

| Group | Landed | Remaining |
|---|---|---|
| Product docs | mission, requirements, architecture, roadmap, backlog | trace milestones to requirements (M-1.24) |
| Standards | git, sdd, testing, performance, security, build, portability, code-structure | the five code standards (M-1.5), review (M-1.23) |
| Gates | commit-msg, reviewed, milestone-review, build-index, which-standards | drift, tests-kept, layering, sans-io, core-contract, unsafe, budget, file-size, readmes, hot-path-bench, portability |
| Skills | adr, milestone, milestone-review, next-task, research, review, spec, tdd | contract-change (M-1.31) |
| Loops | inner (per commit), outer (per milestone) | negative-test every gate (M-1.15), the completion condition itself (M-1.16) |
| Plan | `milestones/README.md`, this file, the roadmap rewrite (M-1.40); the M0/M1/M2/M3/M10 plans (M-1.41) | the other eleven plans (M-1.42, M-1.43) |

## Completion condition

```bash
scripts/gates/m-1-complete.sh
```

Asserts that every non-negotiable in `AGENTS.md` names a script that exists and
passes, that `tests/gates/negative.sh` proves each gate can fail, and that no
rule remains marked "not yet enforced" except rule 3, which cannot be.

⚠️ **Rule 3 is the permanent exception** — "never claim a test passes without
having run it" is a rule about what an agent says, and nothing checks a claim
against an intention. Every other rule rests on it. The completion condition
asserts it is *marked* unenforceable rather than pretending otherwise.

## Risks and open questions

- ⚠️ **This milestone is at 44 tasks, more than twice the limit `sdd.md` now
  states** — and four of them are the commits writing that limit down. It
  is the evidence for the rule rather than an exception to it. Splitting it now
  would renumber tasks the git history already cites, so the cost is paid once,
  here, and every milestone after it is planned under the cap.
- ⚠️ **A gate nothing invokes is a preference.** `.git/hooks` is not tracked, so
  a fresh clone runs none of these until M-1.14 makes a checkout inherit them.
  Until then the enforcement is real only on machines where it was installed by
  hand.
- ⚠️ **Two constants cannot be chosen here, and one task is blocked on that.**
  `check-budget.sh` is **M-1.12**, but its acceptance criterion ("suite over
  budget fails") needs NFR-56's number, and the number needs a workspace to
  measure — so the backlog records it as *blocked rather than guessed*, and the
  roadmap assigns NFR-56 to M0. ⚠️ **`m-1-complete.sh` must not require it.**
  That gate asserts every *non-negotiable* names a passing script, and
  `check-budget.sh` is named by no non-negotiable in `AGENTS.md`; wiring it in
  anyway would deadlock M-1 behind a milestone that comes after it. NFR-55's
  coverage gate has no M-1 task at all and is straightforwardly M0's.
- **The reviewer's independence is bought by the harness, not by the artifact.**
  The diff hash binds a verdict to bytes; it does not prove the reviewer was not
  the author. Doc 21 §4–5, and stated in `AGENTS.md` rather than implied away.

## Notes for the boundary review

⚠️ **No commit in M-1 has been read as a whole** — `reviews/` holds nothing but
its README. The milestone's commit count is deliberately not written here; it
moves with every task, and `scripts/milestone-review.sh coverage` is the only
thing that should be asked for it. What is true is that the coverage is zero:
the outer loop this milestone built is not yet discharged against the milestone that built it. That
review is the first real use of `milestone-review.sh`, and it should pay
particular attention to two things this milestone's own history flags:

1. **Nine instances of the same `set -o pipefail` SIGPIPE defect across six
   scripts**, including inside the fix for another instance of it. That is a
   property of the idiom, not carelessness, and it belongs in whatever shell
   rules `portability.md` grows.
2. **Six of seven review rounds on M-1.37 found a defect on a path that had
   been documented but never walked.** *A failure path nobody has walked is a
   claim, not a feature* — the gate-shaped form of non-negotiable 3.

---
title: "M5 — Compaction and retention"
description: >
  Read when opening M5. Compaction triggered on read amplification, retention that fires on idle partitions, and the GC safety inequality that makes deletion safe against a stale reader.
tags: [product, milestones, planning, m5]
---

# M5 — Compaction and retention

**Kind:** functional ·
**Depends on:** M3, M10 ·
**Serves:** FR-33, FR-34, FR-35

## Goal

Historical reads stay bounded in cost, data ages out on schedule including on
partitions nobody is writing to, and an object is deleted only when no reader
can still reference it.

⚠️ **Compaction here is a metadata mechanism as much as an I/O one**, and that
is the part usually underestimated. At ~40 bytes per index entry and the
modelled rate the index grows ~14 GB/day and ~97 GB over a 7-day retention,
which does not fit in an agent's memory. Object *count* is a coordinator cost;
object *bytes* are a cloud-bill cost, and they want different mechanisms.

## Depends on

**M3** for the index compaction rewrites, **M10** because FR-35's acceptance
criterion is an invariant test on a race — a stale reader against a deleter —
and that is not testable by hand.

## Decisions required first

| Decision | Open question | Background |
|---|---|---|
| Compaction cadence at high partition counts | **#18** | Manifest PUT cost is linear in partitions × cadence and **fixed regardless of throughput**: at 100k partitions, a 60 s cadence is roughly $720/day against ~$24/day at 30 minutes. Interacts with M6's snapshot cadence |
| Retention on idle partitions | **#22** | Time-driven retention must fire on partitions nobody writes to — a per-partition cost. Candidates are lazy evaluation on next access or folding into a daily sweep; ⚠️ **neither is worked through** |
| ~~`max_metadata_staleness`~~ | ~~**#11**~~ | **resolved 2026-08-23, `ADR-0021`: 5 s** — set in M3, consumed here. `deletion_delay` and the inequality's other two terms stay M5's to set |

## Provisional tasks

1. `read_amp(partition, range)` computed from the index — the trigger input.
   ⚠️ **Never trigger on object count.**
2. Planner: select where read amplification exceeds a threshold (start at 8–16)
   **and** the range is older than the latency SLO, so tail data still served
   from cache is never rewritten.
3. Per-plan cost estimate and budget gate; refuse or defer plans over budget.
4. Streaming merge executor: exactly one additional GET per input regardless of
   its size, bounded memory, no random seeks.
5. Output layout regrouped by topic then partition into contiguous ranges, so
   one partition-fetch becomes one ranged GET.
6. Multipart output writer at the part size the cost model is built on.
   ⚠️ **Carries a deferral from `M3`, which had it from `M1`**
   (`roadmap.md`'s deferred-into-a-later-milestone table; `ADR-0013`,
   re-targeted by `ADR-0026`): the streaming writer sealed with a
   `Precondition` that doc 04 §5 motivates multipart with. `M1` deferred it for
   want of a caller and `M3` deferred it again because the caller it was
   promised did not need one — `M3.13`'s flush knows its size when the write
   starts, and `ADR-0020` point 4 forbids a conditional write on the offset
   stream. **This task is the first genuine one**: a merge output's final size
   is not known until the merge finishes. ⚠️ It inherits the whole of
   `ADR-0013`'s problem rather than a solved one — `object_store` still cannot
   condition a `CompleteMultipartUpload` through its public API (re-checked
   against 0.14.1 at `M3.12`), and both live mechanisms, hand-rolled SigV4
   signing and `HttpConnector` interception, were rejected in `M1` for reasons
   that have not moved. Choosing between them is this task's, with a caller in
   hand.
7. Server-side range copy for whole-range moves — PUT-class cost, zero egress.
   ⚠️ Does not apply to every compaction type; do not budget zero egress for all.
   ⚠️ **Carries a deferral from `M1.18`** (ADR-0015; `roadmap.md`'s
   deferred-into-a-later-milestone table): `object_store` exposes server-side
   range copy for neither S3 (`UploadPartCopy` support is `pub(crate)`-only
   and whole-object-only even internally) nor GCS (`compose` does not exist
   in the crate at all — tracked upstream as
   `apache/arrow-rs-object-store#121`, open since 2023-10-20). This task
   inherits the actual decision: hand-roll signed requests now that a real
   caller and cost model exist, depend on `object_store` if #121 has closed
   by then, or accept a client-side copy for whichever compaction types this
   task's own warning above already says zero-egress does not have to cover.
8. **Composite objects**: an object holding pointers to live components and a
   copy of their index entries, collapsing N objects into one from the
   coordinator's view with zero data movement. The reference system bolted this
   on at 90% of an object-count ceiling; **build it from the start**.
8a. **Re-key the index per object rather than per `(object, partition)`** —
   doc 15 §7's coarse index, with partition→range left to the object's own
   footer. ⚠️ **Carries a deferral from `M3`** (`roadmap.md`'s
   deferred-into-a-later-milestone table, recorded by `M3.8`): `M3.6` built the
   two-tier *shape* and its own doc comment says the cheap tier is still at the
   expensive granularity. Doc 14 §3 prices the difference at four orders of
   magnitude — ~4M entries/s and ~160 MB/s for per-`(object, partition)`
   entries against ~400/s and ~16 KB/s for per-object ones — and it is a
   difference of *granularity*, not of field width, so `M3.6`'s narrower
   `ObjectRef` does not touch it. ⚠️ **It belongs here rather than in `M3`
   because it is the same fact compaction is about**: `M3.md`'s Risks section
   says the ~97 GB over a 7-day retention "is what forces M5's compaction to be
   a metadata mechanism as much as an I/O one", and composite objects in task 8
   above already collapse N objects into one from the coordinator's view —
   re-keying is what makes that collapse show up in the index's size.
   ⚠️ **And a dropped cache's refill trigger, for the same reason**
   (`roadmap.md`'s table, recorded by `M3.25`): a reader whose index was
   dropped is refilled only by the next commit through the coordinator, so a
   partition nobody writes to stays cold. The only thing that would drop a
   cache is the quota below, so in `M3` this was a trigger for an event nothing
   raises.
   ⚠️ **And it receives NFR-11's enforcement with it** (`roadmap.md`'s deferral
   table, recorded by `M3.11`). `M3` was to bound index growth and could not:
   at per-`(object, partition)` keying a ceiling can only be met by evicting,
   and eviction gives back range a rebuild cannot restore — replaying the log
   reproduces the same count and sheds the same entries again, so the degraded
   mode's own recovery is a loop that cannot converge. And nothing in M3 ever
   shrinks the count, because retention and compaction are this milestone's, so
   a ceiling there is terminal the moment it is reached. At this task's keying the index is three
   orders of magnitude smaller and the bound is either unnecessary or cheap, so
   the quota, its alarm and its degraded mode are this task's to define —
   `M3` ships only the number — `MaterializedIndex::entries()` — and NFR-11
   stays `M7`'s to verify, as `roadmap.md`'s coverage table always had it.
9. **Range-coverage check before every commit** — verify every input offset
   range is covered by the outputs. Cheap insurance on the one operation that
   can silently lose data.
10. Atomic commit at the coordinator: one transaction swapping input refs for
    output refs. ⚠️ Compaction must never affect correctness, only efficiency.
11. `compaction_epoch` on `ObjectRef`. An older-epoch cached ref stays **valid**
    through the delay window — degradation, not a cliff.
12. Compaction-job claiming by CAS on object storage. ⚠️ A legitimate
    low-frequency control-plane use; explicitly not the offset stream.
13. Time-based retention driven from `ts_min`/`ts_max` in the index, so the
    decision needs no object-storage access.
14. Size-based retention: per-partition byte budget, oldest first, reconciled
    with time retention — the tighter wins.
15. **Retention on idle partitions**, per the ADR above (FR-33).
16. `trim(new_start_offset)` as the only partition-level retention primitive —
    advancing the logical start is metadata-only.
17. Object liveness: deletable only when **every** partition slice inside it is
    dead. ⚠️ Otherwise one slow-expiring partition pins a whole object
    indefinitely, which is what makes repacking a *retention* requirement.
18. Three-state lifecycle — live → logically deleted → deletion queue — with a
    TTL sweep, idempotent commit, and a per-round batch cap **plus
    backpressure**. An uncapped queue that grows once creation outruns deletion
    is a documented pathology in a peer system. ⚠️ **A per-key delete refusal
    from `oqueue-store` is `Transient` and says nothing about why** —
    `ADR-0030`: the S3 error code is erased before `oqueue-store`'s
    `classify()` ever sees it, so "will this key ever delete" has to be
    answered from *repetition across sweeps* here, not from a classification
    the layer below cannot provide.
19. **The GC safety inequality**, in code and as an invariant test:
    `deletion_delay > max_metadata_staleness + max_in_flight_fetch_duration +
    clock_skew`, with its three enablers — non-reusable object IDs, an explicit
    **bounded** staleness that stops cache serving when exceeded, and
    404 ⇒ refresh ⇒ retry.
20. Orphan reconciliation: a daily mark-and-sweep driven by a storage inventory
    rather than by paginated listing. ⚠️ **The one legitimate use of listing.**

21. **The 55 surviving mutants `M4.32` found** (from M4, `roadmap.md`'s
    deferred table, `M4.46`). ⚠️ **`scripts/check-mutants.sh --full` is a CI
    step nobody had ever run** — it was a step in `gates.yml` then and is
    `.github/workflows/mutants.yml`'s nightly job since `M4.51`, which moved
    it after `M4.32` measured the pass at roughly six hours against the 23 s
    the per-push placement had been argued from — and running it once gave
    2294 mutants, 60
    missed, 1498 caught, 724 unviable, 12 timeouts — five argued, so 55 of the
    survivors are unargued and that step is red. 42 are in `oqueue-codec` (14
    `compress.rs`, 13 `attributes.rs`), 6 in `oqueue-core`, 4 in
    `oqueue-broker`, 3 in `oqueue-checksum`, none in `bin/oqueue`. ⚠️ **And 12
    timeouts nothing looks at**: the gate harvests `MISSED` lines only. ⚠️ **M5 because it opens next**, not
    because the code is M5's — `M1`'s eleven rows→`M2` and `M3`'s six→`M10` are
    the precedent. ⚠️ **Each killed or argued on its own merits**: 55 entries
    added to `baselines/mutants.txt` to turn a gate green is what
    non-negotiable 2 forbids for a threshold, read across to a suppression
    list, and `testing.md` rule 17 calls that file one nobody grows quietly.

22. **Five rows M4 opened after its own decomposition and did not work** —
    `M4.70`, `M4.71`, `M4.72`, `M4.80`, `M4.81` (from M4, `roadmap.md`'s
    deferred table, `M4.75`). ⚠️ **The first handoff made by rule rather than
    by somebody noticing**: `M4.73` wrote the disposition test — a finding
    reopens its milestone only if acting on it changes what a client, an
    operator or another tenant observes, or makes a gate able to fail where a
    standard says it must — and `M4.75`'s round applied it to its own eight
    findings, reopening four and handing two on; `M4.70`, `M4.71` and `M4.72`
    were filed by `M4.64`'s round and dispositioned the same way. None of the
    five meets the test.
    ⚠️ **Two are decisions rather than sweeps.** `M4.80`: no shell constant in
    this repository is covered by a completeness scan, so
    `check-fencing-seam.sh`'s new ceiling declares a weakening direction
    nothing enforces and `MIN_VARIANTS` has been in the same position since
    `M4.36` — the answer is a scan or a recorded decision that shell gates are
    outside non-negotiable 2's reach. `M4.81`: whether `code-structure.md`
    rule 16's 500-line limit means non-Rust files at all, given that thirteen
    tracked ones exceed it and two are `Cargo.lock`s. ⚠️ Allowlisting the six
    *scripts* is the answer that looks right and is not — it leaves the lock
    files, `backlog.md`, `roadmap.md` and three research documents unrecorded,
    and those are what make it a scope question. The other three are
    prose: a sweep of claims M4's commit bodies recorded (`M4.70`), an
    `AGENTS.md` sentence naming a direction `check-portability.sh`
    deliberately does not check (`M4.71`), and a doc comment attached to the
    wrong function (`M4.72`). ⚠️ **`M5.0` re-derives them as its own rows and
    marks each `dissolved` with a pointer**, `M2.0`'s and `M10.0`'s shape.

⚠️ **And a log start offset a consumer can actually reset to** (`M3.22`).
`M3.22` gave a reaped read the honest answer — `OFFSET_OUT_OF_RANGE` after a
refresh — but the escape from it is `ListOffsets(EARLIEST)`, which this broker
answers with a literal `0` because nothing deletes anything yet. Once this
milestone's reaper does, a consumer with `auto.offset.reset=earliest` gets
out-of-range, asks for the earliest offset, is told `0`, seeks there, and is
told out-of-range again: a livelock spending a page of GETs per turn. What
breaks it is a real log start offset published by whatever performs the
deletion, and reported in both the `Fetch` response's `log_start_offset` and
`ListOffsets(EARLIEST)`. ⚠️ **`listoffsets.rs`'s `EARLIEST` arm is the one
place that has to change**, and it says so.

## Completion condition

```bash
scripts/gates/m5-complete.sh
```

Asserts: read amplification after compaction is within bound (FR-34); an idle
partition's data is deleted on schedule with no write to trigger it (FR-33); and
the GC safety inequality holds as an invariant test under M10's simulation, with
a stale reader racing a deleter (FR-35).

## Risks and open questions

- ⚠️ **Compaction does not always pay**, and the plan must not assume it does.
  Three named cases where it does not: pure tail consumption served from cache,
  retention shorter than one compaction interval, and data already contiguous.
- ⚠️ **A nonzero 404-on-read rate means the deletion delay is too short.** It
  must be a metric, not a swallowed retry. This is the single loudest signal
  that the inequality is mis-parameterized.
- **The break-even model and the 8–16 threshold are synthesis, not measurement.**
  Treat them as starting defaults that M14 replaces with numbers.
- **Lifecycle-policy expiry cannot deliver sub-day retention precision** —
  daily evaluation then queued asynchronous deletion. Anything with an SLA is
  application-driven deletion.
- ⚠️ **BYOK breaks compaction planning, and M8 depends on this being handled.**
  Compaction must plan **within a key domain**, workers need unwrap capability
  for every topic they touch, and a revoked KEK blocks compaction of that
  topic's data — which needs a defined behaviour rather than a stuck queue.
  Doc 10 #39, #37 (encryption). M5 must not close without either that path or an explicit,
  recorded deferral into M8.
- **Deferred:** key-based log compaction (two sequential passes per input) and
  Express→Standard tiering, if #7 resolves that way.

# M13.17 local gate evidence

Source tree: `27a73e5e438031818cb2ac4eadea1b749e45fb66` (`M13.17: restore portable release CI gates`).

The installed pre-commit hook ran `scripts/docker-test.sh gate` in the resource-capped container before creating that commit. The command exited 0. The hook reported the Rust format/clippy/test gate, per-crate coverage, backend matrix, mutation gate, and structural gates as passed.

The coverage check was also run directly with `scripts/docker-test.sh scripts/check-coverage.sh`; it exited 0 and reported: `9 crate(s) checked, lowest oqueue-crypto at 94.13%, floor 85%, 3 excluded by name`. The gate serializes only the instrumented coverage build; its 85% floor and exclusions were unchanged.

The contained negative-gate suite reported `185 gate(s) exercised, 0 failed to fail as expected`. `scripts/check-conformance-matrix.sh` reported that pinned conformance images use Quay.

The NFR-56 timing measurement was not established by this Windows Docker Desktop run: `check-budget` explicitly skipped the timing measurement because bind-mount timing there is not portable evidence. This record makes no claim that the NFR-56 timing leg ran. Native CI evidence and the aggregate M13 completion gate remain M13.18.

## Staged amendment verification

After moving this evidence note to the product-doc root and correcting the backlog link, `scripts/docker-test.sh gate` was rerun against the staged amendment on 2026-09-23 and exited 0. Requirements tracing, Rust format/clippy/tests, coverage, backend matrix, and mutation checks all passed. The only limitation remained the explicit Windows Docker Desktop NFR-56 timing skip described above; no acceptance threshold was skipped or changed.
